model image_processing {
    id        String   @unique @default(uuid())
    key      String
    email     String   @unique
    password  String
    login_limit Int @default(0)
    upload_limit Int @default(0)
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
